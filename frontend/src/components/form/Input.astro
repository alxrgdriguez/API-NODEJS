---

const { label, id, type, icono, placeholder } = Astro.props;
---

<div>
    <label for={`${id}`} class="block text-sm font-medium text-gray-700">{label}</label>
    <div class="mt-1 relative rounded-md shadow-sm">
        <div class="absolute inset-y-0 left-0 pl-3 flex items-center pointer-events-none">
            <i class={`${icono} text-gray-400`}></i>
        </div>
        <input type={type} id={`${id}`} required class="block w-full pl-10 pr-3 py-2 border border-gray-300 rounded-md leading-5 bg-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 sm:text-sm" placeholder={placeholder}>
    </div>
</div>